mixin flash()
    -if (messages.success)
        div(
            class="alert alert-success"
            data-time="2500"
            show-alert
        ) #{messages.success}
    -if (messages.failed)
        div(
            class="alert alert-danger"
            data-time="2500"
            show-alert
        ) #{messages.failed}